import { Container, Typography } from '@mui/material';

const Groceries = () => {
    return (
      <Container>
        <Typography variant="h5">Groceries Page</Typography>
      </Container>
    );
  }
  
  export default Groceries;

