import { Container, Typography } from '@mui/material';

const Events = () => {
  return (
    <Container>
      <Typography sx={{ fontWeight: "bold", fontSize: "30px", margin: "3em 11em" }} variant="h5">No Event At The Moment</Typography>
    </Container>
  );
}

export default Events;

