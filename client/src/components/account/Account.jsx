import { Container, Typography } from '@mui/material';

const Account = () => {
    return (
      <Container>
        <Typography variant="h5">Account Page</Typography>
      </Container>
    );
  }
  
  export default Account;

